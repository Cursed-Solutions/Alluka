name: Lint Actions

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
  workflow_dispatch:

jobs:
  actionlint:
    uses: FasterSpeeding/Piped/.github/workflows/lint.yml@adc711961a6c2e5eb5b6c6389103b736356dc8e6
    with:
      sessions: "verify-markup spell-check lint slot-check"
