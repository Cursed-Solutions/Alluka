name: Lint Actions

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
  workflow_dispatch:

jobs:
  actionlint:
    uses: FasterSpeeding/Piped/.github/workflows/lint.yml@25d4c5b8d7cf42764b3a978de34cad31bbe53323
    with:
      sessions: "verify-markup spell-check lint slot-check"
