name: Publish a new release

on:
  release:
    # Only use the types keyword to narrow down the activity types that will trigger your workflow.
    types:
      - published

jobs:
  publish:
    permissions:
      id-token: write

    uses: FasterSpeeding/Piped/.github/workflows/publish.yml@f824d61c518eb6a04b59e6b02980e5ce0fd80a50
    secrets: inherit
