name: Publish a new release

on:
  release:
    # Only use the types keyword to narrow down the activity types that will trigger your workflow.
    types:
      - published

jobs:
  publish:
    permissions:
      id-token: write

    uses: FasterSpeeding/Piped/.github/workflows/publish.yml@12f12b2c1f7aef76182a0337ae9c577661cf6aae
    secrets: inherit
