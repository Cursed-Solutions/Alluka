name: Publish a new release

on:
  release:
    # Only use the types keyword to narrow down the activity types that will trigger your workflow.
    types:
      - published

jobs:
  publish:
    permissions:
      id-token: write

    uses: FasterSpeeding/Piped/.github/workflows/publish.yml@0af587d7dd3dbda851dca9b36704a8913ce9b410
    secrets: inherit
