name: Publish a new release

on:
  release:
    # Only use the types keyword to narrow down the activity types that will trigger your workflow.
    types:
      - published

jobs:
  publish:
    permissions:
      id-token: write

    uses: FasterSpeeding/Piped/.github/workflows/publish.yml@45039ff4ec17015d4963e6cc9e1c6d7a21add80a
    secrets: inherit
