name: Publish documentation

on:
  workflow_call:
  release:
    # Only use the types keyword to narrow down the activity types that will trigger your workflow.
    types:
      - published

jobs:
  publish-docs:
    uses: FasterSpeeding/Piped/.github/workflows/release-docs.yml@66d53e2dfe7a90bdac76106bc79c4a02aecd0446
    secrets: inherit
