name: Publish documentation

on:
  workflow_call:
  release:
    # Only use the types keyword to narrow down the activity types that will trigger your workflow.
    types:
      - published

jobs:
  publish-docs:
    uses: FasterSpeeding/Piped/.github/workflows/release-docs.yml@076d0e3ac85d243cb9ca2123b1144364ce7f8a4b
    secrets: inherit
