name: Verify type-completeness

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
  workflow_dispatch:

jobs:
  verify-types:
    uses: FasterSpeeding/Piped/.github/workflows/verify-types.yml@2d96ccdfe342b4b489f6ff753ef3ecb49629fade
