name: Verify type-completeness

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
  workflow_dispatch:

jobs:
  verify-types:
    uses: FasterSpeeding/Piped/.github/workflows/verify-types.yml@7f5789a5b500b5ea084370b88f9ff7716de03d65
