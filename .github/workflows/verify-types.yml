name: Verify type-completeness

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
  workflow_dispatch:

jobs:
  verify-types:
    uses: FasterSpeeding/Piped/.github/workflows/verify-types.yml@12f12b2c1f7aef76182a0337ae9c577661cf6aae
